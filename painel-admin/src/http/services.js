import { services as auth } from '@/modules/auth'
import { services as config } from '@/pages/config'
import { services as watchlist } from '@/pages/watchlist'
import { services as metadados } from '@/pages/metadados'

export default {
  auth,
  config,
  watchlist,
  metadados
}
